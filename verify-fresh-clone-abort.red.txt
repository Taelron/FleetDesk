Date: 2026-08-27T03:15:00Z

Both runs below use the reproducible negative control the approved plan
specifies: FRESH_CLONE_ABORT_MAKEFILE selects which Makefile the target
copies into its non-repository test directory before running
verify-fresh-clone against it. Anyone can re-run either command from the
repo root; this file is a record of one run, not the only proof.

--- Command: make verify-fresh-clone-abort (current recipe; default
    FRESH_CLONE_ABORT_MAKEFILE = Makefile) ---

    | verify-fresh-clone FAIL — could not clone this repository into /tmp/tmp.UM3D1iRXHM/tmpdir/tmp.FEvQPHUvsG
    | make[1]: *** [Makefile:212: verify-fresh-clone] Error 1
verify-fresh-clone-abort OK — aborted (rc 2) naming the clone failure, no fallthrough into make verify, temp directory cleaned up

Exit status: 0

--- Command: git show 321273b^:Makefile > /tmp/tae92-prefix.Makefile &&
    make verify-fresh-clone-abort FRESH_CLONE_ABORT_MAKEFILE=/tmp/tae92-prefix.Makefile
    (pre-TAE-79-review-fix recipe at 321273b^, which clones without
    checking git clone's exit status) ---

    | make[2]: *** No rule to make target 'verify'.  Stop.
    | verify-fresh-clone FAIL — make verify did not pass in the clone
    | make[1]: *** [Makefile:192: verify-fresh-clone] Error 1
FAIL — the failure does not name the clone
FAIL — the recipe carried on past the failed clone and ran make verify in an empty directory (TAE-92 regression)

Exit status: 2
